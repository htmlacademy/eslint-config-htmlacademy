const isTrue = true;

const doEnything = () => {};

if (isTrue) {
  const coordinateX = 3;
  const coordinateY = 6;

  doEnything(coordinateX, coordinateY);
}

const coordinateZ = 5;

function doSomething() {}

throw new Error(coordinateZ, doSomething);
